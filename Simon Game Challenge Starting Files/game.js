var buttonColours = ["red", "blue", "green", "yellow"];

function nextSequence(){
    var randomNumber = Math.random();  //creating a random number
    randomNumber = randomNumber * 4;   //making it be between 0 and 3
    randomNumber = Math.floor(n);

}

var randomChosenColour = 